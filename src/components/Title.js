import React from 'react'

function Title({heading, subHeading}) {
    return (
        <div>
            <h1>{heading}</h1>
            <p>{subHeading}</p>
        </div>
    )
}

export default Title
